package com.lin.heroic_spirit_spell.effect;

import io.redspace.ironsspellbooks.effect.MagicMobEffect;
import net.minecraft.world.effect.MobEffectCategory;

/** Grants temporary block-phasing behavior via IncorporeityEvents. */
public final class IncorporeityEffect extends MagicMobEffect {
    public IncorporeityEffect(MobEffectCategory category, int color) {
        super(category, color);
    }
}
